{
  "name": "saas-ecommerce-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "api"
  ],
  "scripts": {
    "dev:web": "npm run dev --prefix ./web",
    "dev:api": "npm run dev --prefix ./api",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
